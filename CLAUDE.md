1.所有新增或修正的程式, 在完成後必需重新檢視程式碼做完整的語法+引用檢查, 確認不會有問題才算完成
2.所有異動調整完需同步至說明文件
3.寫批次檔或需終端機執行的指令時, 要用全英文, 避免編碼錯誤
4.與使用者的溝通則需使用中文
5.本專案（music-splitter）已授權 git commit 及 git push，完成異動後直接同步，不需再詢問使用者
6.在修改路徑相關程式碼之前，必須先用 Bash 工具實際查出檔案的真實路徑（如 os.listdir、os.walk、find），不可用猜測或假設的路徑。